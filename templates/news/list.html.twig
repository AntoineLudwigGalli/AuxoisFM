{% extends 'base.html.twig' %}
{% block title %}Liste des articles{% endblock %}

{% block body %}
    
    <div class="row text-center mt-5">
        <h1>Liste des articles</h1>
    </div>
      
            
            {# Affichage du menu de navigation seulement s'il y a des articles #}
            {% if articles is not empty %}
                <div class="row">
                    <div class="col-12 my-3 text-center">
                        <div class="d-inline-block">
                            {{ knp_pagination_render(articles) }}
                        </div>
                    </div>
                </div>
            {% endif %}
            
            {% for article in articles %}
                
              
                <div class="row">
                    <div class="col-10 offset-1 d-flex flex-row flex-wrap">
                        {% for article in articles %}
                        <div class="card mx-1 mb-1 col-3">
                            <div class="inner-card d-flex flex-column align-items-center">
                                <h2 class="text-center">{{ article.title }}</h2>
                                <img src="{{ asset("images/articles_covers/" ~ article.coverPicture) }}" class="img-fluid" alt=""/>
                                <p> {{ article.content|striptags|excerpt(20) }} </p>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                </div>
{#                    <div class="col-12">#}
{#                        #}
{#                        <div class="card my-3">#}
{#                            #}
{#                            <div class="card-header">#}
{#                                <a href="{{ path('news_article_view', {'id': article.id, 'slug': article.slug#}
{#                                }) }}" class="text-decoration-none"> <i class="far fa-newspaper#}
{#                                    me-2"></i>#}
{#                                    {{ article.title }} </a>#}
{#                            </div>#}
{#                            #}
{#                            <div class="card-body">#}
{#                                #}
{#                                <div class="mb-2">#}
{#                                    #}
{#                                    {{ article.content|striptags|excerpt(100) }}<a class="text-decoration-none"#}
{#                                            href="{{ path ('news_article_view', {'id': article.id, 'slug': article.slug }) }}">Lire#}
{#                                        la suite</a>#}
{#                                #}
{#                                </div>#}
{#                                #}
{#                                <footer class="blockquote-footer my-2">Publié le#}
{#                                    <strong>{{ article.publicationDate|date('d/m/Y à H\\hi') }}</strong> par#}
{#                                </footer>#}
{#                                #}
{#                                {% if is_granted('ROLE_ADMIN') %}#}
{#                                    <div class="mt-3">#}
{#                                        <a onclick="return confirm('Êtes-vous sur de vouloir modifier cet article ?')"#}
{#                                                href="{{ path('news_article_edit', {'id': article.id}) }}"#}
{#                                                class="text-decoration-none text-warning me-3">#}
{#                                            <i class="fa-solid  fa-pen-to-square me-2"></i>#}
{#                                            Modifier#}
{#                                        </a>#}
{#                                        #}
{#                                        <a onclick="return confirm('Êtes-vous sur de vouloir supprimer cet article ?')"#}
{#                                                href="{{ path('news_article_delete', {'id': article.id, 'csrf_token' : csrf_token('blog_publication_delete_' ~ article.id) }) }}"#}
{#                                                class="text-decoration-none text-danger">#}
{#                                            <i class="fa-solid fa-trash-can me-2"></i>#}
{#                                            Supprimer#}
{#                                        </a>#}
{#                                    </div>#}
{#                                #}
{#                                {% endif %}#}
{#                            #}
{#                            </div>#}
{#                        #}
{#                        </div>#}
{#                    #}
{#                    </div>#}
{#                #}
{#                </div>#}
{#            #}
            {% else %}
                
                <div class="row">
                    <p class="col-12 text-center alert alert-warning">Aucune publication à afficher</p>
                </div>
            
            {% endfor %}
            {# Affichage du menu de navigation seulement s'il y a des articles #}
            {% if articles is not empty %}
                <div class="row">
                    <div class="col-12 my-3 text-center">
                        <div class="d-inline-block">
                            {{ knp_pagination_render(articles) }}
                        </div>
                    </div>
                </div>
            {% endif %}
{% endblock %}